# Runtime dependencies (mirror of pyproject [project.dependencies]).
# The canonical, pinned source of truth is uv.lock; this file exists for
# `pip install -r requirements.txt` in environments without uv.
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
pydantic-settings>=2.2
httpx>=0.27
tenacity>=8.2
structlog>=24.1
typer>=0.12
jinja2>=3.1
openpyxl>=3.1
pandas>=2.2
pyyaml>=6.0
sse-starlette>=2.1
python-multipart>=0.0.9
